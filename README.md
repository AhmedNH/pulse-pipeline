# pulse-pipeline
A real-time data aggregation pipeline in Python: fetches weather and crypto market data from public APIs, stores time-series snapshots in SQLite, and exposes results through a FastAPI REST server and a terminal dashboard.
